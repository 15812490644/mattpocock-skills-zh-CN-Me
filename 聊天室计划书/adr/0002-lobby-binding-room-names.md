# 随机大厅 + 首条消息绑定房间

agent 启动后、聊天室接通前无法给用户发任何消息，所以房间名不能由 agent 预分配。决定：执行计划书的 AI 在生成两个文件时烘焙一个随机大厅地址（两个文件同一份）；用户打开聊天室自己起房间名，作为首条消息发到大厅；watcher 守在大厅，看到即绑定，随后双方切到该房间的入向/出向频道。放弃了"agent 起名再告知用户"（被约束 3 直接排除）。随机大厅使不同部署之间互不串话，房间名只需在大厅内唯一。
